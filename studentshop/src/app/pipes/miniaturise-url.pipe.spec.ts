import { MiniaturiseUrlPipe } from './miniaturise-url.pipe';

describe('MiniaturiseUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new MiniaturiseUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
